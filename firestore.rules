rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow only authenticated content owners access
    match /Players/{userId}/{subPath=**} {
      allow read, write: if request.auth != null && (userId == "plyr_" + request.auth.uid)
    }
  }
}